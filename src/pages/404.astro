---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Page Not Found - LAB Chain" description="The page you're looking for doesn't exist.">
  <section class="not-found">
    <div class="container">
      <div class="not-found-content">
        <div class="error-code">
          <span class="digit">4</span>
          <div class="logo-circle">
            <svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
              <defs>
                <linearGradient id="gradient404" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#CE1126" />
                  <stop offset="50%" stop-color="#002868" />
                  <stop offset="100%" stop-color="#CE1126" />
                </linearGradient>
              </defs>
              <circle cx="50" cy="50" r="48" fill="url(#gradient404)" />
              <circle cx="50" cy="50" r="20" fill="#FFFFFF" opacity="0.95" />
              <text x="50" y="56" text-anchor="middle" font-family="Arial, sans-serif" font-size="24" font-weight="bold" fill="#002868">L</text>
            </svg>
          </div>
          <span class="digit">4</span>
        </div>

        <h1>Page Not Found</h1>
        <p>Oops! The page you're looking for seems to have wandered off the chain. It might have been moved, deleted, or never existed.</p>

        <div class="action-buttons">
          <a href="/" class="btn btn-primary">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
              <polyline points="9 22 9 12 15 12 15 22"></polyline>
            </svg>
            Go Home
          </a>
          <a href="/docs" class="btn btn-secondary">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
              <polyline points="14 2 14 8 20 8"></polyline>
              <line x1="16" y1="13" x2="8" y2="13"></line>
              <line x1="16" y1="17" x2="8" y2="17"></line>
              <polyline points="10 9 9 9 8 9"></polyline>
            </svg>
            Documentation
          </a>
        </div>

        <div class="helpful-links">
          <h3>Helpful Links</h3>
          <div class="links-grid">
            <a href="/rpc" class="link-card">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path>
              </svg>
              <span>RPC Endpoints</span>
            </a>
            <a href="/boot-nodes" class="link-card">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
                <line x1="8" y1="21" x2="16" y2="21"></line>
                <line x1="12" y1="17" x2="12" y2="21"></line>
              </svg>
              <span>Boot Nodes</span>
            </a>
            <a href="/beacon-nodes" class="link-card">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
                <polyline points="2 17 12 22 22 17"></polyline>
                <polyline points="2 12 12 17 22 12"></polyline>
              </svg>
              <span>Beacon Nodes</span>
            </a>
            <a href="/community" class="link-card">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
              </svg>
              <span>Community</span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .not-found {
    min-height: calc(100vh - var(--header-height) - 200px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 80px 0;
  }

  .not-found-content {
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }

  .error-code {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    margin-bottom: 32px;
  }

  .digit {
    font-size: clamp(80px, 15vw, 140px);
    font-weight: 800;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
  }

  .logo-circle {
    width: clamp(80px, 15vw, 140px);
    height: clamp(80px, 15vw, 140px);
    animation: float 3s ease-in-out infinite;
  }

  .logo-circle svg {
    width: 100%;
    height: 100%;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  h1 {
    font-size: clamp(1.5rem, 4vw, 2rem);
    margin-bottom: 16px;
    color: var(--text-primary);
  }

  .not-found-content > p {
    font-size: 16px;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 32px;
  }

  .action-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 48px;
  }

  .action-buttons .btn {
    padding: 14px 28px;
  }

  .helpful-links {
    padding-top: 48px;
    border-top: 1px solid var(--border-color);
  }

  .helpful-links h3 {
    font-size: 14px;
    font-weight: 600;
    color: var(--text-tertiary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 20px;
  }

  .links-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
  }

  .link-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    padding: 20px 16px;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    color: var(--text-secondary);
    transition: all var(--transition-fast);
  }

  .link-card:hover {
    background: var(--bg-tertiary);
    border-color: var(--color-red);
    color: var(--color-red);
    transform: translateY(-2px);
  }

  .link-card span {
    font-size: 13px;
    font-weight: 600;
  }

  @media (max-width: 640px) {
    .links-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .action-buttons {
      flex-direction: column;
    }

    .action-buttons .btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
