---
import Layout from '../layouts/Layout.astro';

const pageTitle = 'Community | LAB Chain';
const pageDescription = 'Join the LAB Chain community. Share your nodes, contribute to the network, and help build decentralized infrastructure for Laos.';
---

<Layout title={pageTitle} description={pageDescription}>
  <main class="community-page">
    <section class="hero">
      <div class="container">
        <h1>Community</h1>
        <p class="hero-desc">
          Help build LAB Chain's decentralized infrastructure by sharing your nodes with the community.
        </p>
      </div>
    </section>

    <section class="actions-section">
      <div class="container">
        <div class="actions-grid">
          <a href="/community/submit-node" class="action-card">
            <div class="action-icon">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <line x1="12" y1="8" x2="12" y2="16"></line>
                <line x1="8" y1="12" x2="16" y2="12"></line>
              </svg>
            </div>
            <div class="action-content">
              <h2>Share Your Node</h2>
              <p>Submit your RPC endpoint, boot node, or beacon node to help strengthen the LAB Chain network.</p>
            </div>
            <div class="action-arrow">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <polyline points="12 5 19 12 12 19"></polyline>
              </svg>
            </div>
          </a>

          <a href="/community/request-status" class="action-card">
            <div class="action-icon">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
              </svg>
            </div>
            <div class="action-content">
              <h2>Check Request Status</h2>
              <p>Track the status of your node submission using your tracking ID.</p>
            </div>
            <div class="action-arrow">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <polyline points="12 5 19 12 12 19"></polyline>
              </svg>
            </div>
          </a>
        </div>

        <div class="info-section">
          <h3>How It Works</h3>
          <div class="steps-grid">
            <div class="step-card">
              <div class="step-number">1</div>
              <h4>Submit Your Node</h4>
              <p>Fill out the form with your node details including endpoint URL, type, and contact information.</p>
            </div>
            <div class="step-card">
              <div class="step-number">2</div>
              <h4>Get Tracking ID</h4>
              <p>After submission, you'll receive a unique tracking ID to monitor your request status.</p>
            </div>
            <div class="step-card">
              <div class="step-number">3</div>
              <h4>Review Process</h4>
              <p>Our team will review your submission to ensure it meets network requirements.</p>
            </div>
            <div class="step-card">
              <div class="step-number">4</div>
              <h4>Get Listed</h4>
              <p>Once approved, your node will be publicly listed and available to all LAB Chain users.</p>
            </div>
          </div>
        </div>

        <div class="benefits-section">
          <h3>Why Share Your Node?</h3>
          <div class="benefits-grid">
            <div class="benefit-card">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
              </svg>
              <h4>Help the Community</h4>
              <p>Provide reliable infrastructure for developers and users building on LAB Chain.</p>
            </div>
            <div class="benefit-card">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
              </svg>
              <h4>Get Recognition</h4>
              <p>Your node will be publicly credited, showcasing your contribution to the network.</p>
            </div>
            <div class="benefit-card">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
              </svg>
              <h4>Strengthen Decentralization</h4>
              <p>More nodes mean a more resilient and decentralized network for everyone.</p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .community-page {
    padding-top: var(--header-height);
  }

  .hero {
    padding: 64px 0;
    background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
    border-bottom: 1px solid var(--border-color);
  }

  .hero h1 {
    font-size: 48px;
    font-weight: 700;
    margin-bottom: 16px;
  }

  .hero-desc {
    font-size: 18px;
    color: var(--text-secondary);
    max-width: 600px;
  }

  .actions-section {
    padding: 64px 0;
  }

  .actions-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
    margin-bottom: 64px;
  }

  .action-card {
    display: flex;
    align-items: center;
    gap: 24px;
    padding: 32px;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    transition: all var(--transition-normal);
  }

  .action-card:hover {
    border-color: var(--color-red);
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .action-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 72px;
    height: 72px;
    background: rgba(206, 17, 38, 0.1);
    border-radius: var(--radius-lg);
    color: var(--color-red);
    flex-shrink: 0;
  }

  .action-content {
    flex: 1;
  }

  .action-content h2 {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 8px;
    color: var(--text-primary);
  }

  .action-content p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .action-arrow {
    color: var(--text-tertiary);
    transition: all var(--transition-fast);
  }

  .action-card:hover .action-arrow {
    color: var(--color-red);
    transform: translateX(4px);
  }

  .info-section {
    margin-bottom: 64px;
  }

  .info-section h3,
  .benefits-section h3 {
    font-size: 24px;
    font-weight: 600;
    margin-bottom: 32px;
    text-align: center;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  .step-card {
    padding: 24px;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    text-align: center;
  }

  .step-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: var(--color-red);
    color: white;
    border-radius: 50%;
    font-size: 18px;
    font-weight: 700;
    margin-bottom: 16px;
  }

  .step-card h4 {
    font-size: 16px;
    font-weight: 600;
    margin-bottom: 8px;
    color: var(--text-primary);
  }

  .step-card p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .benefit-card {
    padding: 32px;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    text-align: center;
  }

  .benefit-card svg {
    color: var(--color-red);
    margin-bottom: 16px;
  }

  .benefit-card h4 {
    font-size: 16px;
    font-weight: 600;
    margin-bottom: 8px;
    color: var(--text-primary);
  }

  .benefit-card p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  @media (max-width: 1024px) {
    .steps-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 36px;
    }

    .hero-desc {
      font-size: 16px;
    }

    .actions-grid {
      grid-template-columns: 1fr;
    }

    .action-card {
      flex-direction: column;
      text-align: center;
    }

    .action-arrow {
      display: none;
    }

    .steps-grid {
      grid-template-columns: 1fr;
    }

    .benefits-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
